import { FC } from 'react';

// Guard
interface DropdownProps {
    className: string;
};

// Components

const Dropdown: FC<DropdownProps> = () => {
    return (
        <>
        </>
    );
};

export default Dropdown;